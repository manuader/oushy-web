Imágenes del carrusel "el feed".

Poné acá las publicaciones que quieras mostrar y listalas en
`src/content/sections.ts` → `feedPosts`, con la ruta `/feed/<archivo>`.

Recomendado: JPG o PNG, relación 4:5 (vertical, como Instagram),
lado mayor ~1200px. Next las optimiza y sirve en AVIF/WebP.
